# Apply this manifest after Helm renders the template.
---
{{- if .Capabilities.APIVersions.Has "external-secrets.io/v1/ExternalSecret" }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: application-secret
{{- end }}
